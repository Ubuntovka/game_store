language: python
python:
  - "3.8"
  - "3.9"
  - "3.10"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - coverage run -m unittest discover gs_app/tests
  - coverage report