language: python
python:
  - "3.9"
  - "3.10"
services: mongodb
addons:
    srcclr: true
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - python -m unittest gs_app/tests/*.py